@model SalesPlanningMonitorng.Barang

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Barang</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.id_barang)

        <div class="form-group">
            @Html.LabelFor(model => model.nama_barang, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.nama_barang)
                @Html.ValidationMessageFor(model => model.nama_barang)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.harga, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.harga)
                @Html.ValidationMessageFor(model => model.harga)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.kategori, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.kategori)
                @Html.ValidationMessageFor(model => model.kategori)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
